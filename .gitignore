# OS junk
.DS_Store

# Obsidian per-machine UI state (churns constantly; not knowledge)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# Python
__pycache__/
*.pyc

# CRM is local-only (personal relationship data; never sync to GitHub or the cloud routine)
crm/

# Personal profile (identity + derived-from-private-data; local-only)
profile/

# Financial data — local-only, never synced
finance/
