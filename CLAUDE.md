# Second Brain — LLM Wiki Schema (Claude Code)

The schema for this vault is the single source of truth in **`AGENTS.md`** (vault root).
It defines the layout, page conventions, source-type rules, the two special files
(`index.md`, `log.md`), and the ingest / query / lint / CRM workflows.

**Read `AGENTS.md` now and follow it exactly.** Do not keep a second copy of the schema
here — this file is only a pointer, so the two can never drift.
